# MeikiPop Swift GUI

Minimal macOS status-bar frontend prototype.

Run it from the repository root:

```sh
nix develop -c swift run --package-path apps/gui-swift
```

The application adds a MeikiPop item to the macOS menu bar. Its menu currently
contains only **Quit MeikiPop**.
