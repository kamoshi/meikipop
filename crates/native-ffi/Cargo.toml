[package]
name = "meikipop-native-ffi"
version = "0.1.0"
edition = "2024"
publish = false

[lib]
name = "meikipop_native_ffi"
crate-type = ["rlib", "staticlib"]

[dependencies]
meikipop-native = { path = "../native" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
